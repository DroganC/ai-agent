# 是否启用 Mock 数据（不设则默认 true）
# 开发默认用 Mock；生产可设为 true 做演示或联调前自测
VITE_USE_MOCK=true
# VITE_USE_MOCK=false

# 接口基础路径（关闭 Mock 时生效）
# VITE_API_BASE=/api/v1

# 前端部署基准路径（子路径部署时必设）
# 例如部署到 https://example.com/come/game/ 时设为 /come/game/
# 不设则默认为 /，适用于部署在域名根路径
# VITE_BASE=/come/game/
