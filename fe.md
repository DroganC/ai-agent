# PRD提示词：AI工程大脑工单智能派单可视化项目
## 1. 项目背景与目标
开发一个“AI工程大脑”可视化交互页面，核心目标是模拟工厂IT运维工单从产生、流入AI大脑、智能分析后自动派发给员工坐席的全流程，页面交互需极致丝滑，数据流转效果具备真实感，直观展现AI工程大脑的工单智能调度能力。

## 2. 技术框架与环境约束（强制）
### 2.1 核心技术栈
- 前端框架：React 18（使用Function Component + Hooks语法，如useState/useEffect/useReducer）
- 构建工具：Vite 7.x
- 状态管理：Redux Toolkit（RTK，React生态主流状态管理方案，替代原错误的Vite表述）
- 动画实现：CSS3 Transition/Animation + GSAP（可选，用于复杂路径动画）
- UI基础：原生CSS + CSS变量 + PostCSS（禁止使用Ant Design/Element Plus等重型UI库，仅允许引入normalize.css做样式重置）
- 依赖版本：Node.js ≥ 20.0.0，npm ≥ 10.0.0，react ≥ 18.2.0，@reduxjs/toolkit ≥ 2.0.0，react-redux ≥ 9.0.0

### 2.2 技术限制
- 禁止使用jQuery等老旧库，所有交互基于原生JS/React 18 Hooks API实现；
- 动画优先使用CSS3实现，仅路径动画等复杂场景可引入GSAP（体积≤100KB）；
- 页面无后端接口依赖，所有工单数据、坐席状态均为前端模拟生成；
- 代码需符合ES6+规范，使用ESLint（React官方airbnb规则）做代码校验；
- 组件编写遵循React 18最佳实践，使用TypeScript（可选，增强类型校验）。

## 3. 项目目录结构（强制）

- 目录结构清晰
- 组件代码解耦清晰

## 4. 页面核心布局要求
- 整体布局：页面为16:9宽屏适配，背景为科技感浅灰/深蓝色渐变，无冗余元素，视觉焦点集中在中间的AI大脑核心区域。
- 核心区域：屏幕正中央放置“AI工程大脑”可视化组件，需具备动态呼吸/脉冲效果（每秒轻微缩放1-2px），大脑主体为3D简约风格（非写实），带有科技蓝/紫色光效，大脑外围有微弱的数据流环绕动画。
- 左侧区域：AI大脑左侧以放射状连接“各工程模块”（至少包含生产工程、质检工程、仓储工程、设备工程4类），每个工程模块为独立卡片，卡片带有工程名称+简约图标，与AI大脑之间有半透明的动态连线（默认浅灰色，有工单流转时变为高亮蓝色）。
- 右侧区域：AI大脑右侧以网格状排列“员工坐席模块”（至少显示8-10个坐席），每个坐席包含坐席编号、员工头像（占位）、当前状态（空闲/忙碌/离线），坐席与AI大脑之间同样有半透明动态连线（默认浅灰色，派单时变为高亮绿色）。
- 工单流转路径：工厂（工程模块）→ 工单 → AI大脑 → 派单 → 员工坐席，路径需有明确的视觉动线。

## 5. 核心交互与动效要求（丝滑体验）
### 5.1 工单产生动效
- 随机从左侧某一工程模块（按预设概率：生产工程60%、质检工程20%、仓储工程15%、设备工程5%）生成“IT运维工单”可视化元素（矩形卡片，含工单编号、问题类型、生成时间）。
- 工单从工程模块沿连线向AI大脑移动，移动速度匀速（3秒/单），移动过程中有轻微的光影拖尾效果，同一时间最多显示3个待处理工单。

### 5.2 AI大脑处理动效
- 工单到达AI大脑后，大脑光效增强（脉冲频率加快至0.5秒/次），显示“处理中”状态，处理时长随机（2-4秒）。
- 处理完成后，大脑弹出简短的“派单逻辑提示”（如“匹配空闲坐席”“按技能匹配”），提示语显示1秒后消失。

### 5.3 工单派单动效
- AI大脑处理完成后，工单沿连线向右侧某一空闲坐席移动，移动速度与来向一致（3秒/单），派单规则：优先派给“空闲”状态坐席，无空闲则暂存于AI大脑（显示“排队中”），空闲坐席释放后自动派发。
- 工单到达坐席后，坐席状态变为“忙碌”，卡片停留在坐席区域，显示“处理中”，5-8秒后工单消失，坐席恢复“空闲”状态。

### 5.4 全局交互要求
- 所有动效无卡顿、无延迟，帧率稳定在60fps；
- 工单流转过程支持暂停/继续（页面右下角悬浮控制按钮）；
- 鼠标悬浮在工单/工程/坐席上时，显示对应详情弹窗（无遮挡核心动效）。

## 6. 模拟数据要求（真实感）
### 6.1 工单数据模板
- 工单编号：IT-YYYYMMDD-6位随机数字（如IT-20260226-897654）；
- 问题类型：随机从列表抽取（服务器宕机、网络中断、设备故障、系统卡顿、权限异常、数据同步失败）；
- 生成时间：实时显示当前系统时间（精确到秒）；
- 派单备注：随机生成（如“匹配坐席03（网络运维专长）”“优先派给空闲超5分钟坐席”）。

### 6.2 数据流转频率
- 工单生成频率：平均每5秒生成1个（随机±2秒波动），模拟真实工厂的工单产生节奏；
- 坐席状态切换：每个坐席“忙碌”时长随机（5-8秒），模拟真实的工单处理耗时；
- 数据持久化：页面运行期间，自动记录工单总数、已处理数、派单成功率（显示在页面顶部状态栏）。

## 7. 视觉风格要求
- 整体风格：科技感、简约、轻量化，避免过度复杂的3D效果；
- 色彩体系：主色（科技蓝#165DFF）、辅助色（成功绿#00B42A、警示黄#FF7D00）、中性色（背景灰#F5F7FA、文字灰#333333）；
- 字体：中文使用微软雅黑/思源黑体，数字使用Roboto，层级清晰（标题18px、内容14px、备注12px）。

## 8. 技术适配要求
- 兼容主流浏览器（Chrome/Firefox/Edge）；
- 支持响应式（屏幕宽度≥1200px时完整显示，＜1200px时自适应缩放核心区域）；
- 代码需按指定目录结构组织，组件化拆分符合React 18最佳实践，Redux Toolkit状态管理需按切片（Slice）拆分。